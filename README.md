# Workers Proficiency Assessment System

Система оценки профессиональной пригодности - веб-приложение для обработки отчетов, извлечения метрик и расчета коэффициента профессиональной пригодности с использованием AI.

## Особенности

- 🔐 Аутентификация с JWT и ролевым доступом (ADMIN/USER)
- 👥 Управление участниками и отчетами
- 📊 Ручной ввод метрик из отчетов
- ⚖️ Система весовых таблиц для различных профессиональных деятельностей
- 🤖 Генерация рекомендаций с использованием Gemini API
- 📈 Расчет профессиональной пригодности на основе взвешенных метрик
- 🎨 Современный UI на основе Vue 3 и Naive UI

## Технологический стек

### Backend
- **FastAPI** 0.120.3 - современный веб-фреймворк
- **PostgreSQL** 15+ - основная БД с JSONB поддержкой
- **SQLAlchemy** 2.0 (async) - ORM
- **Alembic** - миграции БД
- **Pydantic** v2 - валидация данных
- **PyJWT** - JWT аутентификация
- **Google Generative AI SDK** - интеграция с Gemini API
- **Redis** - кэширование

### Frontend
- **Vue 3** - прогрессивный JavaScript фреймворк
- **Vite** - сборщик и dev-сервер
- **Pinia** - управление состоянием
- **Vue Router** - маршрутизация
- **Naive UI** - библиотека UI компонентов
- **Axios** - HTTP клиент

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **Nginx Proxy Manager** - (опционально) reverse proxy с TLS

## Быстрый старт

### Предварительные требования

- Перед началом работы обязательно изучите материалы в каталоге `.memory-base`
- Docker и Docker Compose установлены
- Git
- Gemini API ключ(и) (бесплатные) - получить на https://aistudio.google.com/apikey
  - Рекомендуется 2-3 ключа для автоматической ротации

### Установка

1. **Клонируйте репозиторий**

```bash
cd workers-prof
```

2. **Создайте .env файл**

```bash
cp .env.example .env
```

3. **Настройте переменные окружения**

Отредактируйте файл `.env`:

```bash
# ОБЯЗАТЕЛЬНО: Измените секретный ключ
SECRET_KEY=ваш_очень_длинный_и_случайный_секретный_ключ_минимум_32_символа

# ОБЯЗАТЕЛЬНО: Добавьте ваш Gemini API ключ
# Можно несколько ключей через запятую для автоматической ротации:
GEMINI_API_KEY=ключ1,ключ2,ключ3

# Или один ключ (тоже работает):
# GEMINI_API_KEY=ваш_gemini_api_ключ_здесь

# Опционально: настройте порт и пароли БД
POSTGRES_PASSWORD=your_secure_password
APP_PORT=80  # Единый порт для всего приложения (Nginx)
```

4. **Запустите приложение**

```bash
docker-compose up -d
```

При первом запуске:
- PostgreSQL база данных будет создана
- Миграции будут применены автоматически
- Базовые данные (метрики, профессиональные деятельности) будут загружены

5. **Откройте приложение**

Теперь всё доступно через **один порт** (по умолчанию 80):

- **Приложение**: http://localhost
- **API документация**: http://localhost/api/docs
- **API Redoc**: http://localhost/api/redoc
- **Health Check**: http://localhost/api/healthz

> 💡 Nginx автоматически маршрутизирует:
> - `/` → Frontend (Vue)
> - `/api/` → Backend (FastAPI)

### Первый вход

1. Откройте http://localhost:3000
2. Нажмите "Зарегистрироваться"
3. Создайте аккаунт с email и паролем (минимум 8 символов)
4. После регистрации ваш аккаунт будет в статусе "PENDING"

#### Создание администраторской учетной записи

**Способ 1: Использование скрипта (рекомендуется)**

```bash
# Зайдите в контейнер API
docker exec -it workers-prof-api bash

# Запустите скрипт создания администратора
python create_admin.py admin@example.com ваш_пароль

# Выйдите из контейнера
exit
```

**Способ 2: Через БД (если нужно активировать существующего пользователя)**

```bash
# Подключитесь к контейнеру PostgreSQL
docker exec -it workers-prof-postgres psql -U postgres -d workers_prof

# Активируйте пользователя и назначьте роль ADMIN
UPDATE "user"
SET status = 'ACTIVE', role = 'ADMIN', approved_at = NOW()
WHERE email = 'ваш@email.com';

# Выйдите
\q
```

Теперь вы можете войти как администратор и подтверждать других пользователей через UI.

## Использование

### Workflow системы

1. **Регистрация и вход**
   - Зарегистрируйтесь как новый пользователь
   - Дождитесь подтверждения от администратора

2. **Создание участника**
   - Перейдите в раздел "Участники"
   - Нажмите "Добавить участника"
   - Заполните ФИО, дату рождения, внешний ID

3. **Загрузка отчета**
   - Откройте карточку участника
   - Нажмите "Загрузить отчет"
   - Выберите тип отчета (REPORT_1/2/3)
   - Загрузите .docx файл

4. **Ввод метрик**
   - Выберите загруженный отчет в таблице
   - Нажмите "Ввести метрики"
   - Введите значения для каждой метрики (1-10)
   - Сохраните

5. **Создание весовой таблицы (только ADMIN)**
   - Перейдите в раздел "Администрирование"
   - Нажмите "Создать весовую таблицу"
   - Распределите веса для метрик (сумма = 1.0)
   - Активируйте таблицу

6. **Расчет оценки**
   - На странице участника нажмите "Рассчитать оценку"
   - Система рассчитает score_pct и сгенерирует рекомендации через Gemini API
   - Просмотрите результаты в таблице "Результаты оценки"

### API Endpoints

Полная документация доступна по адресу http://localhost:8000/docs

**Аутентификация:**
- `POST /auth/register` - регистрация
- `POST /auth/login` - вход (получение JWT токена)
- `GET /auth/me` - текущий пользователь

**Участники:**
- `GET /participants` - список участников (с поиском)
- `POST /participants` - создать участника
- `GET /participants/{id}` - получить участника
- `PUT /participants/{id}` - обновить участника
- `DELETE /participants/{id}` - удалить участника

**Отчеты:**
- `POST /reports` - загрузить отчет (multipart/form-data)
- `GET /reports/{id}` - получить отчет
- `GET /reports/{id}/download` - скачать файл отчета
- `GET /reports/participant/{participant_id}` - отчеты участника

**Метрики:**
- `GET /metrics/definitions` - определения метрик
- `POST /metrics/reports/{report_id}/metrics` - создать/обновить метрику
- `GET /metrics/reports/{report_id}/metrics` - метрики отчета

**Весовые таблицы (ADMIN):**
- `POST /weights` - создать весовую таблицу
- `GET /weights` - список весовых таблиц
- `POST /weights/{id}/activate` - активировать таблицу

**Скоринг:**
- `POST /scoring` - рассчитать оценку
- `GET /scoring/participant/{participant_id}` - история оценок
- `GET /scoring/{id}` - детали оценки

**Администрирование (ADMIN):**
- `GET /admin/users/pending` - ожидающие подтверждения
- `POST /admin/users/{id}/approve` - подтвердить пользователя
- `POST /admin/users/{id}/disable` - отключить пользователя

## Разработка

### Структура проекта

```
workers-prof/
├── api-gateway/          # Backend FastAPI
│   ├── alembic/          # Миграции БД
│   ├── app/
│   │   ├── core/         # Конфигурация, БД, безопасность
│   │   ├── models/       # SQLAlchemy модели
│   │   ├── schemas/      # Pydantic схемы
│   │   ├── routers/      # API endpoints
│   │   ├── services/     # Бизнес-логика
│   │   └── repositories/ # Слой доступа к данным
│   ├── seed.py           # Начальные данные
│   └── main.py           # Точка входа
├── frontend/             # Frontend Vue 3
│   ├── src/
│   │   ├── components/   # Vue компоненты
│   │   ├── views/        # Страницы
│   │   ├── stores/       # Pinia stores
│   │   ├── services/     # API клиент
│   │   └── router/       # Vue Router
│   └── vite.config.js
├── data/                 # Persistent volumes
│   ├── postgres/
│   ├── redis/
│   └── uploads/
├── docker-compose.yml
├── .env
└── README.md
```

### Локальная разработка

**Backend:**

```bash
cd api-gateway
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# Настройте .env или экспортируйте переменные
export DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5432/workers_prof"
export SECRET_KEY="dev-secret-key"
export GEMINI_API_KEY="your-key"

# Примените миграции
alembic upgrade head

# Загрузите начальные данные
python seed.py

# Создайте администраторскую учетную запись
python create_admin.py admin@example.com ваш_пароль

# Запустите dev сервер
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**Frontend:**

```bash
cd frontend
npm install

# Запустите dev сервер
npm run dev
```

### Создание новой миграции

```bash
cd api-gateway
alembic revision --autogenerate -m "description of changes"
alembic upgrade head
```

## Архитектура

### Модель данных

Полная ER-диаграмма и описание находятся в `.memory-base/Tech details/infrastructure/data-model.md`

Основные таблицы:
- **user** - пользователи системы
- **participant** - участники оценки
- **report** - загруженные отчеты
- **metric_def** - определения метрик
- **extracted_metric** - извлеченные значения метрик
- **prof_activity** - профессиональные деятельности
- **weight_table** - версионированные весовые таблицы
- **weight_row** - веса для метрик
- **scoring_result** - результаты расчетов

### Формула расчета оценки

```
score_pct = Σ(metric_value × weight × 10)
```

где:
- `metric_value` - значение метрики (1-10)
- `weight` - вес метрики (0-1, сумма всех весов = 1.0)
- Результат в процентах (0-100)

### Интеграция с Gemini API

Система использует Gemini API для генерации персонализированных рекомендаций на основе:
- Общего score_pct
- Сильных сторон (метрики >= 8)
- Зон развития (метрики <= 4)

Модель: `gemini-2.0-flash-exp` (можно изменить в .env)

## Конфигурация

### Переменные окружения

См. `.env.example` для полного списка. Основные:

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `SECRET_KEY` | JWT secret (ВАЖНО: изменить!) | - |
| `GEMINI_API_KEY` | Gemini API ключ | - |
| `DATABASE_URL` | PostgreSQL connection string | auto |
| `REDIS_URL` | Redis connection string | auto |
| `DEBUG` | Debug режим | false |
| `API_PORT` | Порт API | 8000 |
| `FRONTEND_PORT` | Порт Frontend | 3000 |

## Безопасность

- JWT токены с настраиваемым временем жизни
- Bcrypt хеширование паролей
- Ролевой контроль доступа (RBAC)
- Защита эндпоинтов по ролям
- CORS настроен для безопасной работы
- Валидация всех входных данных через Pydantic
- SQL injection защита через SQLAlchemy ORM

**ВАЖНО:** Измените `SECRET_KEY` в production окружении!

## Лицензия

MIT

## Поддержка

Для вопросов и проблем используйте GitHub Issues.

---

**Версия:** 0.1.0 (MVP)
**Дата:** 2025
