Код: S3-01 — DOCX parse → артефакты

Цель
- Celery‑задача извлекает `word/media/*` и пишет артефакты.

Описание
- Извлечение встроенных изображений из `.docx` в предсказуемую структуру каталогов отчёта. Статусы отчёта обновляются по результату обработки; ошибки логируются для диагностики.

Шаги
- Парсинг `.docx`, сохранение `image*.png` в артефакты, обновление статуса отчёта.

Тестирование (обязательно)
- Извлечение образцов; корректные статусы при успехе/ошибке; отсутствие утечек файлов.

AC: статус `UPLOADED→EXTRACTED/FAILED`, логи. Зависимости: S1-07.
