Код: S1-11 — Docker Compose (один внешний порт)

Цель
- Экспортировать только `9187:9187`; остальные сервисы — internal.

Описание
- Сконфигурировать Compose так, чтобы наружу был опубликован лишь порт приложения `9187`. БД, Redis, RabbitMQ и worker доступны только во внутренней сети, без `ports:`. Добавить healthchecks и общие тома (reports, db‑data).

Шаги
- Compose для `app/worker/postgres/redis/rabbitmq`.
- Healthchecks, volumes для `reports/` и БД.

Тестирование (обязательно)
- Убедиться, что `docker ps` показывает один опубликованный порт у `app`.
- Проверить сетевую недоступность БД/Redis/RabbitMQ снаружи хоста.

AC: внешне виден только 9187. Зависимости: S1-10.
