Код: S1-04 — Миграции (ядро)

Цель
- Alembic миграции для: `user`, `participant`, `file_ref`, `report`, `prof_activity`.

Описание
- Базовая схема БД для учётных записей, участников, загрузок файлов и отчётов, а также справочника профдеятельностей. Определить индексы/ограничения и уникальности согласно ER‑модели.

Шаги
- Описать модели и ревизии.
- Прогнать `upgrade head` локально.

Тестирование (обязательно)
- Проверить применимость миграций на пустой БД и обратимость `downgrade`.
- Верифицировать ключевые ограничения (unique/index) на тестовых вставках.

AC: миграции проходят, индексы/уникальности заданы. Зависимости: S1-03.
