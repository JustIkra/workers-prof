Код: S3-03 — Логирование/трейсинг

Цель
- Структурные логи с `request_id`, тайминги задач.

Описание
- Ввести структурное логирование для API и воркеров: кореляция по `request_id`, отметки времени, уровни логов, маскирование секретов. Минимальные метрики таймингов задач/эндпоинтов для диагностики.

Шаги
- Мидлварь для `request_id`; форматтер логов; маскирование секретов; метрики таймингов.

Тестирование (обязательно)
- Проверить наличие `request_id` в логах, отсутствие секретов, корректные уровни на ошибках.

AC: ключевые события покрыты; маскирование секретов. Зависимости: S1.
