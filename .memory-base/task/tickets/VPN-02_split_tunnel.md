Код: VPN-02 — Split‑tunnel маршрутизация

Цель
- Направлять трафик к `generativelanguage.googleapis.com` через WG, остальное — в обход.

Описание
- Настроить правила маршрутизации в контейнере так, чтобы только домены Gemini шли через интерфейс WG, а внутренние сети Docker/БД/кэш работали без VPN. Конфигурация управляется переменными окружения.

Шаги
- Прописать маршруты для доменов/подсетей; исключить docker‑сети; добавить скрипт проверки маршрутизации.

Тестирование (обязательно)
- Проверить, что запросы к Gemini идут через `wg0` (traceroute), а к Postgres/Redis — по внутренней сети.

AC: скрипт проверки (traceroute/curl), правила маршрутизации задокументированы. Зависимости: VPN-01.
