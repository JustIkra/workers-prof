Код: VPN-04 — Compose/безопасность

Цель
- `cap_add: NET_ADMIN`, монтирование `config/vpn/wireguard/`, единственный экспорт `9187`.

Описание
- Обеспечить минимально необходимые привилегии контейнера для поднятия WireGuard (NET_ADMIN), смонтировать конфиг и не публиковать наружу сервисы БД/кэша/очередей. Порт приложения — единственный экспортируемый наружу (`9187`).

Шаги
- Добавить `cap_add: NET_ADMIN`, volume для `config/vpn/wireguard/`, проверить отсутствие других `ports:`.

Тестирование (обязательно)
- Внешне доступен только порт `9187`; порты БД/Redis/RabbitMQ не проброшены.

AC: внешний порт только 9187; остальные сервисы без `ports`. Зависимости: S1-11.
