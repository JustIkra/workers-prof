# Руководство по управлению весовыми таблицами

## Обзор

Страница администрирования весовых таблиц доступна по адресу `/admin/weights` (требуется роль ADMIN).

## Функциональность

### 1. Просмотр весовых таблиц

**Основной список:**
- Отображает все весовые таблицы с группировкой по профессиональным областям
- Показывает версию, статус (активна/неактивна), количество компетенций
- Отображает сумму весов с цветовой индикацией:
  - 🟢 Зелёный: сумма = 1.0 (валидная таблица)
  - 🔴 Красный: сумма ≠ 1.0 (невалидная таблица)

**Фильтрация:**
- По профессиональной области (dropdown)
- Кнопка "Обновить" для перезагрузки данных

### 2. Создание весовой таблицы

**Шаги:**
1. Нажать кнопку "Создать весовую таблицу"
2. Выбрать профессиональную область из выпадающего списка
3. Добавить компетенции:
   - Нажать "Добавить компетенцию"
   - Выбрать метрику из списка доступных (с фильтрацией)
   - Указать вес (от 0 до 1 с точностью до 4 знаков)
   - Для удаления компетенции — нажать красную кнопку с корзиной
4. Настроить веса так, чтобы сумма равнялась 1.0000
5. (Опционально) Добавить метаданные (описание версии)
6. Нажать "Создать"

**Валидация:**
- Сумма весов должна равняться 1.0000 (с точностью ±0.0001)
- Все компетенции должны быть уникальными
- Все поля должны быть заполнены
- Кнопка "Создать" активна только при выполнении всех условий

**Визуальный индикатор суммы весов:**
- 🟢 Зелёный: сумма = 1.0 (можно создавать)
- 🟡 Жёлтый: сумма < 1.0 (осталось распределить)
- 🔴 Красный: сумма > 1.0 (превышение)
- Показывает разницу до целевого значения

### 3. Просмотр деталей таблицы

**Информация:**
- Профессиональная область
- Версия таблицы
- Статус (активна/неактивна)
- Дата создания
- Сумма весов

**Таблица компетенций:**
- Код метрики
- Вес (с точностью до 4 знаков)
- Процент от общего (вес × 100%)

**Метаданные:**
- JSON с дополнительной информацией о таблице

### 4. Активация весовой таблицы

**Важно:**
- Для каждой профессиональной области может быть активна только одна версия таблицы
- При активации новой версии текущая активная автоматически деактивируется
- Требуется подтверждение действия

**Способы активации:**
1. Из списка: кнопка "Активировать" для неактивных таблиц
2. Из детального просмотра: кнопка "Активировать" внизу диалога

## Технические детали

### API endpoints

```javascript
// Список всех весовых таблиц (с фильтрацией)
GET /admin/weights?prof_activity_code={code}

// Создание новой весовой таблицы
POST /admin/weights/upload
{
  "prof_activity_code": "meeting_facilitation",
  "weights": [
    { "metric_code": "communication", "weight": 0.18 },
    { "metric_code": "teamwork", "weight": 0.10 },
    ...
  ],
  "metadata": {
    "description": "Версия для Q1 2025"
  }
}

// Активация весовой таблицы
POST /admin/weights/{weight_table_id}/activate
```

### Источник компетенций

Компетенции загружаются из `GET /api/metric-defs?active_only=true`

Список включает все активные метрики из системы, например:
- Эмоциональные: Лидерство, Мотивация достижений, Стрессоустойчивость
- Социальные: Коммуникабельность, Командность, Ориентация на клиента
- Волевые: Организованность, Нормативность, Ответственность
- Когнитивные: Инновационность, Комплексное решение проблем
- Интеллект: Невербальная логика, Вербальная логика, Эрудиция
- И другие (см. отчёты в `.memory-base/Product Overview/User story/`)

### Версионирование

- Каждая новая таблица для профессиональной области получает автоматический инкремент версии
- Версия 1 создаётся автоматически при первой загрузке
- Последующие загрузки увеличивают номер версии

## Примеры использования

### Пример 1: Создание таблицы для "Проведение совещаний"

Согласно документу `Пример_расчет_Проведение_совещаний.docx`:

```json
{
  "prof_activity_code": "meeting_facilitation",
  "weights": [
    { "metric_code": "communication", "weight": 0.18 },
    { "metric_code": "teamwork", "weight": 0.10 },
    { "metric_code": "conflict_low", "weight": 0.07 },
    { "metric_code": "team_role_soul", "weight": 0.08 },
    { "metric_code": "organization", "weight": 0.08 },
    { "metric_code": "responsibility", "weight": 0.07 },
    { "metric_code": "nonverbal_logic", "weight": 0.10 },
    { "metric_code": "info_processing", "weight": 0.05 },
    { "metric_code": "complex_problem_solving", "weight": 0.05 },
    { "metric_code": "morality", "weight": 0.10 },
    { "metric_code": "stress_resistance", "weight": 0.05 },
    { "metric_code": "leadership", "weight": 0.04 },
    { "metric_code": "vocabulary", "weight": 0.03 }
  ],
  "metadata": {
    "description": "Весовая таблица для оценки компетенций при проведении совещаний",
    "source": "Пример_расчет_Проведение_совещаний.docx"
  }
}
```

**Сумма весов:** 1.00 ✅

### Пример 2: Быстрое распределение весов

Для равномерного распределения между N компетенциями:
- 5 компетенций: каждая по 0.2000
- 10 компетенций: каждая по 0.1000
- 13 компетенций (как в примере): веса от 0.03 до 0.18

## Troubleshooting

**Проблема:** Кнопка "Создать" неактивна
- Проверьте сумму весов (должна быть ровно 1.0000)
- Убедитесь, что выбрана профессиональная область
- Проверьте, что все компетенции заполнены и уникальны

**Проблема:** Ошибка "Another active weight table exists"
- Текущая активная версия конфликтует с новой
- Деактивируйте старую версию или используйте API напрямую

**Проблема:** Метрики не загружаются
- Убедитесь, что в системе есть активные определения метрик
- Проверьте доступ к `/api/metric-defs`
- Создайте метрики через соответствующий интерфейс

## См. также

- [Frontend Requirements](./frontend-requirements.md)
- [Development Guidelines](../Development/development_guidelines.md)
- [User Flow](../../Product Overview/User story/user_flow.md)
- [Weight Tables API](../../../api-gateway/app/routers/weights.py)
