# OpenVPN Client Configuration Example
# 
# This is a template configuration file. Replace placeholders with your actual values.
# Never commit real certificates, keys, or configuration files with secrets.

# Client mode
client

# TUN interface (usually tun0)
dev tun

# Protocol (tcp or udp)
proto tcp

# Retry connection indefinitely
resolv-retry infinite

# Don't bind to a specific local port
nobind

# Keep key and tun interface persistent across restarts
persist-key
persist-tun

# Encryption settings
cipher AES-256-GCM
auth SHA512
verb 3

# TLS settings
tls-client
tls-version-min 1.2
key-direction 1
remote-cert-tls server

# DNS servers (optional, may be overridden by server)
# dhcp-option DNS 172.29.172.254
# dhcp-option DNS 1.0.0.1

# Remote server address and port
# Replace with your VPN server details
remote YOUR_VPN_SERVER_IP 443

# CA Certificate
# Replace with your actual CA certificate
<ca>
-----BEGIN CERTIFICATE-----
REPLACE_WITH_YOUR_CA_CERTIFICATE
-----END CERTIFICATE-----
</ca>

# Client Certificate
# Replace with your actual client certificate
<cert>
-----BEGIN CERTIFICATE-----
REPLACE_WITH_YOUR_CLIENT_CERTIFICATE
-----END CERTIFICATE-----
</cert>

# Client Private Key
# Replace with your actual private key (NEVER commit this!)
<key>
-----BEGIN PRIVATE KEY-----
REPLACE_WITH_YOUR_PRIVATE_KEY
-----END PRIVATE KEY-----
</key>

# TLS Authentication Key (if used)
# Replace with your actual TLS auth key
<tls-auth>
-----BEGIN OpenVPN Static key V1-----
REPLACE_WITH_YOUR_TLS_AUTH_KEY
-----END OpenVPN Static key V1-----
</tls-auth>

# NOTE: redirect-gateway is typically used for full-tunnel mode
# When using split-tunnel (VPN_ROUTE_MODE=domains or cidr), the application
# uses --route-nopull to prevent OpenVPN from changing the default route.
# If you need full-tunnel mode, remove --route-nopull from the bootstrap script
# and uncomment the line below:
# redirect-gateway def1 ipv6 bypass-dhcp

